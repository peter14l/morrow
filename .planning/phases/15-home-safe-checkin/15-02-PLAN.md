---
phase: 15-home-safe-checkin
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/services/geofence_monitor_service.dart
  - lib/features/settings/presentation/screens/home_location_screen.dart
  - test/services/geofence_monitor_service_test.dart
autonomous: true
requirements:
  - HOMESAFE-02
  - HOMESAFE-03

must_haves:
  truths:
    - "Geofence auto-detects when user reaches home location"
    - "Users can manually set home location via Settings UI"
    - "Partner receives notification when user reaches home"
  artifacts:
    - path: "lib/services/geofence_monitor_service.dart"
      provides: "Geofence detection using background location monitoring"
      min_lines: 80
    - path: "lib/features/settings/presentation/screens/home_location_screen.dart"
      provides: "UI for manually setting home location with map picker"
      min_lines: 100
  key_links:
    - from: "home_location_service.dart"
      to: "geofence_monitor_service.dart"
      via: "getHomeLocation() call"
      pattern: "HomeLocationService.*geofence"
---

<objective>
Implement geofence auto-detection when user reaches home location and create Settings UI for manual home location setting.
</objective>

<execution_context>
@D:/oasis/.opencode/get-shit-done/workflows/execute-plan.md
@D:/oasis/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@lib/services/home_location_service.dart
@lib/services/live_location_tracker.dart
@lib/models/location_data.dart
</context>

<feature>
  <name>Geofence Auto-Detection</name>
  <files>lib/services/geofence_monitor_service.dart, test/services/geofence_monitor_service_test.dart</files>
  <behavior>
    - Test: isWithinGeofence(home, current) returns true when within 100m radius
    - Test: isWithinGeofence(home, current) returns false when >100m away
    - Test: monitorGeofence() starts/stops location monitoring
    - Test: onHomeArrival callback triggers when entering geofence
  </behavior>
  <implementation>
    Implement GeofenceMonitorService using Geolocator distance calculation.
    Use 100m radius threshold for "at home" detection.
    Background monitoring with periodic location checks.
    Callback system for home arrival events.
  </implementation>
</feature>

<tasks>

<task type="tdd">
  <name>Task 1: Create GeofenceMonitorService (TDD)</name>
  <files>lib/services/geofence_monitor_service.dart, test/services/geofence_monitor_service_test.dart</files>
  <behavior>
    - Test: isWithinGeofence() returns correct boolean for distance
    - Test: setRadius() changes geofence radius threshold
    - Test: startMonitoring() begins location checks
    - Test: stopMonitoring() stops location checks
    - Test: onHomeArrived callback fires when entering geofence
  </behavior>
  <action>
    Create lib/services/geofence_monitor_service.dart:
    - Constructor takes optional radius (default: 100 meters)
    - isWithinGeofence(HomeLocation home, LocationData current): bool
    - startMonitoring(): starts periodic location checks (every 30 seconds)
    - stopMonitoring(): stops monitoring
    - onHomeArrived: Function callback for geofence entry
    - Uses HomeLocationService to get home location
    - Uses Geolocator.getCurrentPosition() for current location
    - IMPORTANT: Only monitors when app is active (foreground) - background in Plan 15-03 if needed
    
    Create test/services/geofence_monitor_service_test.dart:
    - Test distance calculations with known coordinates
    - Test callback invocation
  </action>
  <verify>
    <automatic>flutter test test/services/geofence_monitor_service_test.dart</automatic>
  </verify>
  <done>GeofenceMonitorService with TDD tests passing, threshold detection working</done>
</task>

<task type="auto">
  <name>Task 2: Create HomeLocationSettingsScreen</name>
  <files>lib/features/settings/presentation/screens/home_location_screen.dart</files>
  <action>
    Create lib/features/settings/presentation/screens/home_location_screen.dart:
    
    UI Components:
    - AppBar with "Home Location" title
    - Current location display (or "Not set" state)
    - "Set Location" button to trigger map picker
    - "Clear Location" button (if location is set)
    - Map view showing current home location (if set)
    
    Functionality:
    - On "Set Location" tap: Open map picker (use location_picker package or custom)
    - After picker returns: Call HomeLocationService.setHomeLocation()
    - On "Clear Location" tap: Call HomeLocationService.clearHomeLocation()
    - On screen load: Display current home location from HomeLocationService
    
    Map Picker Implementation:
    - Use maps_launcher or simple lat/lon input as fallback
    - Fallback: Manual coordinate entry with address lookup
    - NOTE: Use existing location picker patterns from live_location_tracker
    
    Privacy: Show prominent note "Your home location is stored only on this device and never sent to servers"
  </action>
  <verify>
    <automated>grep -n "home_location_screen.dart" lib/features/settings/presentation/screens/ -r</automated>
  </verify>
  <done>HomeLocationSettingsScreen created with set/clear functionality</done>
</task>

<task type="auto">
  <name>Task 3: Wire Settings navigation</name>
  <files>lib/routes/app_router.dart</files>
  <action>
    Add route for HomeLocationSettingsScreen:
    - Route path: '/settings/home-location'
    - Material page wrapper
    - Import HomeLocationSettingsScreen
    
    Update settings_screen.dart to navigate to this route:
    - The tile added in Plan 15-01 now navigates to actual screen
  </action>
  <verify>
    <automated>grep -n "home-location" lib/routes/app_router.dart</automated>
  </verify>
  <done>Settings Home Location tile navigates to correct screen</done>
</task>

<task type="auto">
  <name>Task 4: Integrate Geofence with app lifecycle</name>
  <files>lib/services/app_initializer.dart, lib/services/home_location_service.dart</files>
  <action>
    Add geofence check on app resume:
    - In app_initializer.dart or equivalent, check location on app resume
    - Use GeofenceMonitorService to detect if user is now at home
    - If user arrived home since last check, trigger notification flow
    
    Implementation:
    - Track "lastKnownAtHome" state in PrefsStorage
    - On app resume, compare current location to home
    - If now at home and was not before: trigger home_arrived event
    
    This creates the foundation for notifications in Plan 15-03.
  </action>
  <verify>
    <automated>grep -n "geofence\|atHome" lib/services/app_initializer.dart</automated>
  </verify>
  <done>App resume triggers geofence check</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device→device | Home arrival notifications sent to partner via push notification |
| location→app | Geolocator provides user location to geofence service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---------|----------|-----------|-------------|----------------|
| T-15-03 | Denial of Service | Geofence monitoring | mitigate | Add timeout protection, stop if app background too long |
| T-15-04 | Privacy | Location tracking | accept | Foreground only, user's explicit home setting |
</threat_model>

<verification>
- [ ] GeofenceMonitorService correctly detects 100m radius
- [ ] HomeLocationSettingsScreen has set/clear functionality
- [ ] Settings navigation works
- [ ] App resume triggers geofence check
</verification>

<success_criteria>
- User can set/clear home location via settings UI
- Geofence detects home arrival with 100m accuracy
- App automatically detects home arrival on resume
</success_criteria>

<output>
After completion, create `.planning/phases/15-home-safe-checkin/15-02-SUMMARY.md`
</output>