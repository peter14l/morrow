---
phase: 15-home-safe-checkin
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/core/storage/prefs_storage.dart
  - lib/models/home_location.dart
  - lib/services/home_location_service.dart
  - test/services/home_location_service_test.dart
autonomous: true
requirements:
  - HOMESAFE-01
  - HOMESAFE-06

must_haves:
  truths:
    - "Home location is stored locally only, never sent to server"
    - "Users can manually set their home location in Settings"
    - "Home location persists across app restarts"
  artifacts:
    - path: "lib/models/home_location.dart"
      provides: "Home location data model with latitude, longitude, and metadata"
      min_lines: 30
    - path: "lib/services/home_location_service.dart"
      provides: "Local storage service for home location with get/set/clear operations"
      min_lines: 50
    - path: "test/services/home_location_service_test.dart"
      provides: "TDD tests for home location service"
      min_lines: 60
  key_links:
    - from: "prefs_storage.dart"
      to: "home_location_service.dart"
      via: "SharedPreferences instance"
      pattern: "PrefsStorage.*home_location"
---

<objective>
Create local storage infrastructure for home location and models using TDD. Home location is stored locally via SharedPreferences only - never sent to server (per privacy requirement).
</objective>

<execution_context>
@D:/oasis/.opencode/get-shit-done/workflows/execute-plan.md
@D:/oasis/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@lib/core/storage/prefs_storage.dart
@lib/models/location_data.dart
@lib/services/live_location_tracker.dart

# Key patterns from codebase:
- PrefsStorage pattern: wrapper around SharedPreferences with typed read/write
- LocationData pattern: latitude, longitude, timestamp, address, isLive
- LiveLocationTracker: Uses Geolocator for location, stores location metadata
</context>

<feature>
  <name>Home Location Local Storage</name>
  <files>lib/models/home_location.dart, lib/services/home_location_service.dart, test/services/home_location_service_test.dart</files>
  <behavior>
    - Test 1: getHomeLocation() returns null when no home location is set
    - Test 2: setHomeLocation(latitude, longitude) persists location to SharedPreferences
    - Test 3: clearHomeLocation() removes location and getHomeLocation() returns null
    - Test 4: setHomeLocation with null parameters throws validation error
    - Test 5: Distance calculation from current position to home location works correctly
  </behavior>
  <implementation>
    Create HomeLocation model with location_data serialization to JSON.
    Create HomeLocationService wrapping PrefsStorage.getString/setString.
    Implement distance calculation using Haversine formula from LocationData.
  </implementation>
</feature>

<tasks>

<task type="tdd">
  <name>Task 1: Create HomeLocation model (TDD)</name>
  <files>lib/models/home_location.dart, test/models/home_location_test.dart</files>
  <behavior>
    - Test: HomeLocation can be serialized to/from JSON for SharedPreferences storage
    - Test: HomeLocation calculates distance to another location correctly
    - Test: HomeLocation null checks for latitude/longitude
  </behavior>
  <action>
    Create HomeLocation model in lib/models/home_location.dart:
    - Properties: latitude (double), longitude (double), timestamp (DateTime), address (String?)
    - Factory constructor from JSON map
    - toJson() method returning Map<String, dynamic>
    - distanceTo(HomeLocation other) method using Haversine formula
    - copyWith() method for immutable updates
    
    Create test/models/home_location_test.dart with TDD RED phase:
    - expect(HomeLocation(lat, lon).latitude).equals(lat)
    - expect(HomeLocation.fromJson(toJson())).equals(original)
  </action>
  <verify>
    <automated>flutter test test/models/home_location_test.dart</automated>
  </verify>
  <done>HomeLocation model exists with serialization, distance calculation, and tests passing</done>
</task>

<task type="tdd">
  <name>Task 2: Create HomeLocationService (TDD)</name>
  <files>lib/services/home_location_service.dart, test/services/home_location_service_test.dart</files>
  <behavior>
    - Test: getHomeLocation() returns null when no location set
    - Test: setHomeLocation(37.7749, -122.4194) stores and retrieves correctly
    - Test: clearHomeLocation() removes stored location
    - Test: hasHomeLocation() returns correct boolean state
  </behavior>
  <action>
    Create lib/services/home_location_service.dart:
    - Uses PrefsStorage for local-only persistence (KEY: 'home_location')
    - getHomeLocation(): returns HomeLocation? from stored JSON
    - setHomeLocation(double lat, double lon, {String? address}): stores to PrefsStorage
    - clearHomeLocation(): removes from PrefsStorage
    - hasHomeLocation(): checks if location exists
    - IMPORTANT: No server calls - local storage only
    
    Create test/services/home_location_service_test.dart:
    - Test all public methods with mock PrefsStorage
  </action>
  <verify>
    <automated>flutter test test/services/home_location_service_test.dart</automated>
  </verify>
  <done>HomeLocationService with TDD tests passing, all methods working correctly</done>
</task>

<task type="auto">
  <name>Task 3: Add Settings UI entry points</name>
  <files>lib/screens/settings_screen.dart</files>
  <action>
    Add "Home Location" tile in Settings under Privacy or General section:
    - Icon: home_outlined or location_on
    - Title: "Home Location"
    - Subtitle: "Set your home address for safe check-in" (or "Not set" if none)
    - On tap: Navigate to new HomeLocationSettingsScreen (to be implemented in Plan 15-02)
    
    This is a forward reference stub - the actual screen comes in Plan 15-02.
    Add settings tile only; do not create the full screen yet.
  </action>
  <verify>
    <automated>grep -n "Home Location" lib/screens/settings_screen.dart</automated>
  </verify>
  <done>Settings screen has Home Location entry point, navigates to placeholder route</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local→server | Home location stays in SharedPreferences, never crosses to Supabase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---------|----------|-----------|-------------|----------------|
| T-15-01 | Tampering | SharedPreferences | mitigate | Encrypt location data before storing (optional: use flutter_secure_storage for sensitive) |
| T-15-02 | Information Disclosure | Local storage | accept | Location stored locally only, user explicitly controls |
</threat_model>

<verification>
- [ ] HomeLocation model has toJson/fromJson working
- [ ] Distance calculation matches expected Haversine results
- [ ] HomeLocationService stores/retrieves correctly via PrefsStorage
- [ ] getHomeLocation() returns null when nothing stored
- [ ] clearHomeLocation() properly removes data
- [ ] Settings UI has Home Location tile added
</verification>

<success_criteria>
- Home location stored ONLY in SharedPreferences (not sent to server)
- All CRUD operations work correctly with TDD tests passing
- Distance calculation correct within 100m tolerance
</success_criteria>

<output>
After completion, create `.planning/phases/15-home-safe-checkin/15-01-SUMMARY.md`
</output>