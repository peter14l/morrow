---
phase: 14-couples-connection
plan: 03
type: execute
wave: 2
depends_on: [14-01]
files_modified: []
autonomous: true
requirements: [COUPLE-03]
user_setup: []

must_haves:
  truths:
    - "Special moments can be saved for the right time"
    - "Future dreams are visualized on a shared map"
    - "Photos become collaborative canvases"
  artifacts:
    - path: "lib/widgets/time_capsule.dart"
      provides: "Delayed delivery with date/location/mood triggers"
    - path: "lib/widgets/dream_map.dart"
      provides: "Google Maps with custom colored pins"
    - path: "lib/widgets/memory_layer.dart"
      provides: "Multi-layer drawing over photos"
  key_links:
    - from: "time_capsule.dart"
      to: "couple_items table"
      via: "trigger_meta for release conditions"
---

<objective>
**Phase 14.3 (The History):** Implement Time Capsules, Dream Maps, and Memory Layering — features for preserving and creating shared history.

Purpose: Allow couples to capture moments that unlock at meaningful times, plan future dreams together, and create collaborative art from memories.

Output: Working history features for long-term relationship growth.
</objective>

<execution_context>
@D:/oasis/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/14-couples-connection/14-CONTEXT.md
@docs/plans/COUPLES_CONNECTION_PLAN.md
@.planning/phases/14-couples-connection/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Digital Time Capsule widget</name>
  <files>lib/widgets/time_capsule.dart</files>
  <action>
- Create TimeCapsule widget for delayed content delivery
- Trigger types:
  - `trigger_date`: Release at specific date/time
  - `trigger_location`: Geofenced release (using geolocator package)
  - `trigger_mood`: Release when both partners set matching mood
- Encryption: Content stays encrypted until ALL trigger keys validated
- UI: Sealed envelope aesthetic, animated "unlock" when triggers met
- Save to couple_items with trigger_meta JSONB
  </action>
  <verify>
<automated>flutter analyze lib/widgets/time_capsule.dart 2>&1 | grep -E "error" | head -5 || echo "OK"</-automated>
  </verify>
  <done>
Capsules created with triggers, release when conditions met.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Dream Map widget</name>
  <files>lib/widgets/dream_map.dart</files>
  <action>
- Create DreamMap widget using google_maps_flutter
- Pin types with colors: Blue (Bucket List), Red (Past Memory), Yellow (Date Idea)
- Add custom marker styles for each type
- Tap pin to view details, long-press to add new pin
- Save pins to couple_items.metadata JSONB
- Include basic place search for adding locations
  </action>
  <verify>
<automated>flutter analyze lib/widgets/dream_map.dart 2>&1 | grep -E "error" | head -5 || echo "OK"</automated>
  </verify>
  <done>
Map displays with colored pins, can add new dream/future items.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Memory Layering canvas</name>
  <files>lib/widgets/memory_layer.dart</files>
  <action>
- Create MemoryLayer widget for collaborative drawing over photos
- Pick photo from gallery as background
- Drawing tools: pen, sticker, text overlay
- Save JSON paths to couple_items.metadata
- Sync drawing via real-time channel (collaborative editing)
- Export combined image to gallery
  </action>
  <verify>
<automated>flutter analyze lib/widgets/memory_layer.dart 2>&1 | grep -E "error" | head -5 || echo "OK"</automated>
  </verify>
  <done>
Can select photo and draw/stick added content on it.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| location services | Privacy-sensitive - validate permission |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|------------|------------|
| T-14-06 | Information Disclosure | Geofence triggers | mitigate | Request location permission, no tracking |
| T-14-07 | Tampering | Shared drawing | accept | Both can edit - low risk |
</threat_model>

<verification>
- [ ] All history widgets compile
- [ ] Geofencing permissions requested properly
</verification>

<success_criteria>
Time capsules unlock at right moments, Dream Maps show future dreams, Memory Layering allows collaborative art.
</success_criteria>

<output>
After completion, create `.planning/phases/14-couples-connection/14-03-SUMMARY.md`