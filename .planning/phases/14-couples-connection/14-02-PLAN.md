---
phase: 14-couples-connection
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified: []
autonomous: true
requirements: [COUPLE-02]
user_setup: []

must_haves:
  truths:
    - "Partner can feel your pulse through haptic feedback"
    - "Candle flickers when partner thinks of you"
    - "Both can synchronize breathing for co-regulation"
    - "Mood visible without asking"
  artifacts:
    - path: "lib/widgets/pulse_button.dart"
      provides: "DM header heartbeat sync button with haptics"
    - path: "lib/widgets/flicker_candle.dart"
      provides: "Home screen candle widget with broadcast trigger"
    - path: "lib/widgets/shared_breath.dart"
      provides: "2-minute breathing sync ritual"
    - path: "lib/widgets/mood_glow.dart"
      provides: "Avatar mood indicator with color glow"
  key_links:
    - from: "pulse_button.dart"
      to: "Supabase Broadcast"
      via: "channel.send() for <50ms latency"
---

<objective>
**Phase 14.2 (The Senses):** Implement Pulse (Heartbeat Sync), Flicker, Shared Breath, and Mood Glow — sensory features that bridge physical distance through haptics and visuals.

Purpose: Create real-time sensory connections that let partners feel each other's presence, whether through haptic pulses, visual cues, or synchronized breathing.

Output: Working sensory features that create an intimate digital bridge.
</objective>

<execution_context>
@D:/oasis/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/14-couples-connection/14-CONTEXT.md
@docs/plans/COUPLES_CONNECTION_PLAN.md
@.planning/phases/14-couples-connection/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Pulse (Heartbeat Sync) button</name>
  <files>lib/widgets/pulse_button.dart, lib/screens/dm_screen.dart</files>
  <action>
- Add PulseButton to DM header (beside partner name)
- UI: Expanding concentric ripples in oasisGlow color when held
- Haptics: HapticFeedback.lightImpact in 60bpm rhythmic loop
- Tech: Supabase Broadcast channel for <50ms latency
- On press: Start repeating haptic + broadcast "pulse" event
- On release: Stop transmission
  </action>
  <verify>
<automated>flutter analyze lib/widgets/pulse_button.dart 2>&1 | grep -E "error" | head -5 || echo "OK"</automated>
  </verify>
  <done>
Pressing pulse button sends haptic feedback and broadcasts to partner.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Flicker candle widget</name>
  <files>lib/widgets/flicker_candle.dart, lib/screens/home_screen.dart</files>
  <action>
- Add small candle widget to Home Screen (widget area)
- UI: Candle flame that flickers with warm orange glow (#FFCC00)
- Tech: One-shot Broadcast trigger on tap
- When tapped: Send broadcast event, partner's candle flickers with animation
- Use CustomPainter for flame animation
  </action>
  <verify>
<automated>flutter analyze lib/widgets/flicker_candle.dart 2>&1 | grep -E "error" | head -5 || echo "OK"</automated>
  </verify>
  <done>
Tapping candle sends one-shot trigger to partner's device.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Shared Breath ritual</name>
  <files>lib/widgets/shared_breath.dart</files>
  <action>
- Create 2-minute collaborative breathing ritual
- UI: Two circles (yours + partner's) that must overlap
- Central expanding flower that grows on inhale, stops on exhale
- Haptics: Growing intensity on inhale, sudden stop on exhale
- Partner circle shows their real-time position via Presence
- Goal: Physiological co-regulation
- 4-7-8 breathing pattern (4 inhale, 7 hold, 8 exhale)
  </action>
  <verify>
<automated>flutter analyze lib/widgets/shared_breath.dart 2>&1 | grep -E "error" | head -5 || echo "OK"</automated>
  </verify>
  <done>
Both partners can start Shared Breath and see synchronized circles.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create Mood Glow indicator</name>
  <files>lib/widgets/mood_glow.dart</files>
  <action>
- Create MoodGlow widget for avatar outer glow
- Color mapping: Green (Safe), Blue (Quiet), Red (Needs Space), Yellow (Happy)
- 20px BoxShadow around avatar
- Updates via Presence channel (no polling)
- Add mood selector to settings in Bubble screen
  </action>
  <verify>
<automated>flutter analyze lib/widgets/mood_glow.dart 2>&1 | grep -E "error" | head -5 || echo "OK"</automated>
  </verify>
  <done>
Avatar displays mood glow that partners can see without asking.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| real-time broadcast | Untrusted network - validate all events |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|------------|------------|
| T-14-04 | Denial of Service | Broadcast spam | mitigate | Rate limit pulse/flicker to 1/sec max |
| T-14-05 | Tampering | Mood state | accept | Low risk - personal preference |
</threat_model>

<verification>
- [ ] All widgets compile without error
- [ ] Real-time features wired to Broadcast/Presence
</verification>

<success_criteria>
Partners can feel Pulse, see Flicker, do Shared Breath together, and view Mood Glow.
</success_criteria>

<output>
After completion, create `.planning/phases/14-couples-connection/14-02-SUMMARY.md`