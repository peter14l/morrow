---
phase: 14-couples-connection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [COUPLE-01]
user_setup: []

must_haves:
  truths:
    - "Couples can create and manage a private Bubble (shared space)"
    - "Whispers can be sent as encrypted surprise boxes"
    - "Favor coupons can be created, claimed, and tracked"
  artifacts:
    - path: "supabase/migrations/14-couples-bubble.sql"
      provides: "Database schema for couple_bubbles, couple_items, couple_interactions tables"
      min_lines: 50
    - path: "lib/models/couple_bubble.dart"
      provides: "Bubble data model with encryption support"
    - path: "lib/screens/bubble_screen.dart"
      provides: "Main Bubble UI with avators and interaction"
  key_links:
    - from: "bubble_screen.dart"
      to: "couple_bubble.dart"
      via: "model imports"
      pattern: "import.*couple_bubble"
---

<objective>
**Phase 14.1 (The Core):** Implement Bubble Setup, Wrapped Whispers, and Favor Coupons — the foundational couple connection features.

Purpose: Establish the database schema and core UI for couples to create private shared spaces where they can exchange secret messages and track acts of kindness.

Output: Working Bubble system with encrypted message boxes and favor tracking.
</objective>

<execution_context>
@D:/oasis/.opencode/get-shit-done/workflows/execute-plan.md
@D:/oasis/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-couples-connection/14-CONTEXT.md
@docs/plans/COUPLES_CONNECTION_PLAN.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Supabase schema for couples tables</name>
  <files>supabase/migrations/14-couples-bubble.sql</files>
  <action>
Create migration file with:
- `couple_bubbles` table: (id uuid PK, user_1 uuid, user_2 uuid, weather_mode text, garden_level int, created_at timestamptz)
- `couple_items` table: (id uuid PK, bubble_id uuid FK, type text, payload text (encrypted JSONB), is_unwrapped bool, trigger_meta jsonb, metadata jsonb)
- `couple_interactions` table: (id uuid PK, bubble_id uuid FK, type text, timestamp timestamptz) - add index on (bubble_id, timestamp) with 30-day TTL policy
- RLS policies for data isolation between partners
- Encrypt all couple_items.payload with AES-256-GCM using derived key from PQA-X3DH
  </action>
  <verify>
<automated>psql -f supabase/migrations/14-couples-bubble.sql 2>&1 | grep -E "CREATE|INSERT|ERROR" | head -20</automated>
  </verify>
  <done>
Schema created with all 3 tables and RLS policies. Verify with: SELECT count(*) FROM couple_bubbles; returns 0 (empty but table exists).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create CoupleBubble data model</name>
  <files>lib/models/couple_bubble.dart</files>
  <action>
Create data model with:
- CoupleBubble class with id, user1, user2, weatherMode, gardenLevel, createdAt
- CoupleItem class with id, bubbleId, type (whisper/coupon/treasure/capsule), payload (encrypted), isUnwrapped, triggerMeta, metadata
- CoupleInteraction class with id, bubbleId, type, timestamp
- Enum for BubbleWeather (goldenHour, mistyMorning, northernLights)
- Enum for ItemType (wrapped_whisper, favor_coupon, virtual_treasure, time_capsule)
  </action>
  <verify>
<automated>flutter analyze lib/models/couple_bubble.dart 2>&1 | grep -E "error|warning" | head -10 || echo "Analyze passed"</automated>
  </verify>
  <done>
Model file compiles without errors. Contains all classes and enums exported.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Bubble Screen with Wrapped Whispers UI</name>
  <files>lib/screens/bubble_screen.dart</files>
  <action>
Create bubble_screen.dart with:
- Scaffold with AppBar showing partner name and mood glow indicator
- Body showing two avatar circles (yours + partner's) in center
- FloatingActionButton to "send whisper"
- WrappedWhisper widget: 3D box with "Pull" tab that reveals text on swipe
- FavorCoupons list view: perforated ticket aesthetic, claim button with tearing animation
- Use BoxShadow for 20px mood glow around avatars (green/blue/red/yellow based on mood)
- Implement HapticFeedback.lightImpact for unwrap "click"
  </action>
  <verify>
<automated>flutter analyze lib/screens/bubble_screen.dart 2>&1 | grep -E "error|warning" | head -10 || echo "Analyze passed"</automated>
  </verify>
  <done>
Bubble screen renders with avatars, whisper composer, and coupons list.
  </done>
</task>

<task type="auto">
  <name>Task 4: Wire Bubble to navigation and state</name>
  <files>lib/providers/couple_provider.dart, lib/main.dart</files>
  <action>
- Create CoupleProvider extending ChangeNotifier for bubble state
- Add bubble route in main.dart navigation
- Route: BubbleScreen accessible from Home via "Our Bubble" button
  </action>
  <verify>
<automated>grep -r "bubble" lib/main.dart | head -5</automated>
  </verify>
  <done>
Bubble accessible from main app navigation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Supabase | Untrusted user data crosses here - must validate |
| partner1↔partner2 | Encrypted payloads - E2EE boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|------------|------------|
| T-14-01 | Tampering | couple_items.payload | mitigate | AES-256-GCM encryption, validate schema |
| T-14-02 | Repudiation | couple_interactions | mitigate | Add timestamp index for audit trail |
| T-14-03 | Information Disclosure | RLS policies | mitigate | Test that users cannot read other's data |
</threat_model>

<verification>
- [ ] Migration runs without error
- [ ] flutter analyze passes for new files
- [ ] Bubble screen renders without crash
- [ ] Navigation routes work
</verification>

<success_criteria>
Couples can create a bubble, send encrypted wrapped whispers, and create/claim favor coupons.
</success_criteria>

<output>
After completion, create `.planning/phases/14-couples-connection/14-01-SUMMARY.md`
</output>