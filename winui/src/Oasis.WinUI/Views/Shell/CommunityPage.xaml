<Page
    x:Class="Oasis.WinUI.Views.Shell.CommunityPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:Oasis.WinUI.Core.Converters"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:ImageUrlConverter x:Key="ImageUrlConverter" />
        <converters:DateTimeToStringConverter x:Key="DateTimeToStringConverter" />
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
        <converters:BoolToTextConverter x:Key="MemberButtonTextConverter" />

        <DataTemplate x:Key="PostTemplate">
            <StackPanel Padding="0,10" Spacing="6">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <PersonPicture Width="32" Height="32"
                                   ProfilePicture="{Binding UserAvatar, Converter={StaticResource ImageUrlConverter}}" />
                    <StackPanel Spacing="0" VerticalAlignment="Center">
                        <TextBlock Text="{Binding Username}" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding Timestamp, Converter={StaticResource DateTimeToStringConverter}}"
                                   FontSize="11"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    </StackPanel>
                    <Button Width="28" Height="28" Padding="0" HorizontalAlignment="Right"
                            Background="Transparent" BorderThickness="0">
                        <FontIcon Glyph="&#xE10C;" FontSize="14" />
                        <Button.Flyout>
                            <MenuFlyout>
                                <MenuFlyoutItem Text="Delete" Click="DeletePostMenuItem_Click"
                                                CommandParameter="{Binding}" />
                            </MenuFlyout>
                        </Button.Flyout>
                    </Button>
                </StackPanel>

                <TextBlock Text="{Binding Content}" TextWrapping="Wrap" />

                <Image Source="{Binding ImageUrl, Converter={StaticResource ImageUrlConverter}}"
                       MaxHeight="280" Stretch="Uniform" HorizontalAlignment="Left"
                       Visibility="{Binding ImageUrl, Converter={StaticResource NullToVisibilityConverter}}" />

                <StackPanel Orientation="Horizontal" Spacing="12">
                    <Button Padding="6,4" Background="Transparent" BorderThickness="0"
                            Click="LikeButton_Click" CommandParameter="{Binding}">
                        <StackPanel Orientation="Horizontal" Spacing="4">
                            <FontIcon Glyph="&#xEB51;" FontSize="14" />
                            <TextBlock Text="{Binding Likes}" />
                        </StackPanel>
                    </Button>
                    <Button Padding="6,4" Background="Transparent" BorderThickness="0"
                            Click="CommentButton_Click" CommandParameter="{Binding}">
                        <StackPanel Orientation="Horizontal" Spacing="4">
                            <FontIcon Glyph="&#xE90A;" FontSize="14" />
                            <TextBlock Text="{Binding Comments}" />
                        </StackPanel>
                    </Button>
                </StackPanel>

                <Border Height="1" Margin="0,6,0,0" Background="{ThemeResource DividerStrokeColorDefaultBrush}" />
            </StackPanel>
        </DataTemplate>
    </Page.Resources>

    <Grid Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="4" Margin="0,0,0,8">
            <TextBlock x:Name="CommunityTitle" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock x:Name="CommunityMeta" FontSize="12"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8" Margin="0,0,0,8">
            <Button x:Name="JoinLeaveButton" Click="JoinLeaveButton_Click" />
        </StackPanel>

        <StackPanel Grid.Row="2" Spacing="8" Margin="0,0,0,12">
            <TextBox x:Name="ComposerBox" PlaceholderText="Post in this community..."
                     AcceptsReturn="True" TextWrapping="Wrap" MaxHeight="80" />
            <Button Content="Post" Style="{StaticResource AccentButtonStyle}"
                    HorizontalAlignment="Right" Click="PostButton_Click" />
        </StackPanel>

        <ListView x:Name="PostsList" Grid.Row="3"
                  SelectionMode="None"
                  ItemTemplate="{StaticResource PostTemplate}" />
    </Grid>
</Page>
